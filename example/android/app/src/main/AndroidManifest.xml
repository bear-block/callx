<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <!-- Trigger events from FCM data, example path is payload.type value is call.started -->
      <meta-data android:name="incoming" android:value="type:call.started" />
      <meta-data android:name="ended" android:value="type:call.ended" />
      <meta-data android:name="missed" android:value="type:call.missed" />
      <meta-data android:name="answered_elsewhere" android:value="type:answered_elsewhere" />

      <!-- Fields: where to read values from in FCM data -->
      <meta-data android:name="callId" android:value="callId" />
      <meta-data android:name="callerName" android:value="callerName:Medgate" />
      <meta-data android:name="callerPhone" android:value="callerPhone:1234567890" />
      <meta-data android:name="callerAvatar" android:value="callerAvatar:https://via.placeholder.com/150" />
      <meta-data android:name="hasVideo" android:value="hasVideo:true" />

      <!-- Optional: token event name etc. -->
      <meta-data android:name="showOverLockscreen" android:value="true" />
      <meta-data android:name="requireUnlock" android:value="false" />
      <meta-data android:name="supportsVideo" android:value="true" />
      <meta-data android:name="enabledLogPhoneCall" android:value="true" />
    </application>
</manifest>
